@{
    ViewBag.Title = "ConfigRemove";
}

<h2>Are you sure you want to remove this handler?</h2>
<p>
    @ViewBag.Test
</p>

<button type="button" id="OK">OK</button>
<button type="button" id="Cancel">Cancel</button>

<script type="text/javascript">
    //the function is satrt when the user press the ok button.
    //the function disabled the buttons and call to the HandlerDelete function in the controller
    document.getElementById("OK").onclick = function () {
        document.getElementById("Cancel").disabled = true;
        document.getElementById("OK").disabled = true;
        location.href = "HandlerDelete";
    };
 </script>

<script type="text/javascript">
    //the function is satrt when the user press the cancel button.
    //the function disabled the buttons and call to the Config function in the controller
    document.getElementById("Cancel").onclick = function aa() {
        document.getElementById("Cancel").disabled = true;
        document.getElementById("OK").disabled = true;
        location.href = "Config";
    };
</script>
